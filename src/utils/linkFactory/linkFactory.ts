// example
export const getLinkToRegister = (locale: string = 'ko') => {
  return `/${locale}/register`
}

export const getLinkToForgotPassword = (locale: string = 'ko') => {
  return `/${locale}/forgot-password`
}

export const getLinkBetHistory = (locale: string = 'ko') => {
  return `/${locale}/bet-history`
}

export const getLinkTranscationHistory = (locale: string = 'ko') => {
  return `/${locale}/transaction-history`
}

export const getLinkTransferHistory = (locale: string = 'ko') => {
  return `/${locale}/transfer-history`
}

export const getLinkMyWallet = (locale: string = 'ko') => {
  return `/${locale}/my-wallet`
}

export const getLinkBankAccount = (locale: string = 'ko') => {
  return `/${locale}/bank-account`
}

export const getLinkPromotion = (locale: string = 'ko') => {
  return `/${locale}/promotion`
}

export const getLinkMyPromotion = (locale: string = 'ko') => {
  return `/${locale}/my-promotion`
}

export const getLinkKYCForm = (locale: string = 'ko') => {
  return `/${locale}/KYC`
}

export const getLinkReferral = (locale: string = 'ko') => {
  return `/${locale}/my-referral`
}

export const getLinkReferralDetail = (locale: string = 'ko') => {
  return `/${locale}/my-referral/detail`
}

export const getLinkReferralGroupHistory = (locale: string = 'ko') => {
  return `/${locale}/my-referral/group-referral`
}

export const getLinkReferralGroupSettings = (locale: string = 'ko') => {
  return `/${locale}/my-referral/settings`
}

export const getLinkAffiliateCreate = (locale: string = 'ko') => {
  return `/${locale}/affiliates/`
}
